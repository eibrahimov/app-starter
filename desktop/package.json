{
  "name": "desktop",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "dev": "bash ../scripts/bundle-sidecar.sh --debug && tauri dev",
    "build": "bash ../scripts/bundle-sidecar.sh && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
