// Enumerable Radix Icons allow-list -- the closed set of glyphs the template
// uses, so agents pick deterministically from a known list rather than guessing
// names. To exceed the ~332-icon set, swap @radix-ui/react-icons for lucide-react
// as an approval-gated change (replace all imports; never run both icon libs).
export {
  SunIcon,
  MoonIcon,
  CheckIcon,
  CaretSortIcon,
  ChevronDownIcon,
  CrossCircledIcon,
  CheckCircledIcon,
  ExclamationTriangleIcon,
  MagnifyingGlassIcon,
  PlusIcon,
  TrashIcon,
} from "@radix-ui/react-icons";
