services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      RUST_LOG: info
      DATABASE_URL: "sqlite:///data/app.db?mode=rwc"
    volumes:
      - app-data:/data
    restart: unless-stopped

volumes:
  app-data:
